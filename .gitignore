out*/
tests/*.kl
KLambda/callgraph-*.shen
*.fasl

# Compiled Go binary bundled into the uvx wheel at build time.
pybin/*/_bin/

# Default `go build` output.
/yggdrasil
/yggdrasil_bin

# Local launcher binaries built by integration scripts.
.bin/
